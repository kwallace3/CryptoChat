CREATE TABLE IF NOT EXISTS users (
username VARCHAR(10),
email VARCHAR(30),
pass VARCHAR(10)
);
CREATE TABLE IF NOT EXISTS messages (
mText VARCHAR(100),
userR VARCHAR(10),
sendD DATE,
sendT TIME
);

SHOW TABLES;
